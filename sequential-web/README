Sequential 3
sequential-web

Node modules `aws-sign` and `formidable` are required. They can be installed with `npm`.

./secret.json:
```json
{
	"aws": {
		"accessKeyId": "",
		"secretAccessKey": ""
	},
	"salt": ""
}
```

Domains will have to be updated in server/config.json and uploader/client.js.
